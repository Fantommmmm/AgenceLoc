{% extends 'base.html.twig' %}

{% block title %}Gestion des commandes
{% endblock %}

{% block body %}

	<div class="container text-center"><h1>Modifier Date de Début et de fin de Location</h1></div>


	{{ form_start(commandeForm) }}
	<div class="container text-center">
		{{ form_label(commandeForm.date_heure_depart) }}
		{{ form_widget(commandeForm.date_heure_depart) }}

		{{ form_label(commandeForm.date_heure_fin) }}
		{{ form_widget(commandeForm.date_heure_fin) }}

		<button type="submit" class="btn btn-warning mt-4">Modifier</button>

	</div>

	{{ form_end(commandeForm) }}
{% endblock %}
